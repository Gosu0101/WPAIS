학부생 개발자로서 프로젝트의 뼈대를 잡을 수 있도록, 정의된 비즈니스 로직(7+3 전략, 적응 가속도, 스테이지 퍼스트)을 시스템적으로 구현하기 위한 \*\*기술 명세(Tech Spec)\*\*와 **주요 모듈 설계** 초안을 정리해 드립니다.

## ---

**1\. 기술 명세 (Technical Specification)**

본 시스템은 다수의 프로젝트를 안정적으로 관리하기 위해 **이벤트 기반 아키텍처**와 **객체 지향 설계**를 기본으로 합니다. 111

### **1.1 핵심 기술 스택 (2026 추천)**

* **Language/Framework**: TypeScript, NestJS (Backend), Next.js 16 (Frontend)  
* **Database**: PostgreSQL 18 (관계형 데이터의 정밀한 트래킹에 적합)  
* **State Management**: Temporal.io 또는 BullMQ (비동기 릴레이 공정 및 상태 전이 관리) 2

* **Infrastructure**: AWS ECS Fargate, RDS (관리 포인트 최소화)

### **1.2 시스템 제약 사항 (Constraints)**

* **단위(Unit)**: 모든 작업의 최소 단위는 '페이지(20,000px)'이다. 3

* **의존성(Dependency)**: Page.Background 상태가 DONE이 아니면 Page.LineArt는 수정 불가능하다. 444

* **가중치(Weight)**: $Ep \\le 10$인 경우 기본 소요 시간($T$)에 가중치($W=2.0$)를 곱해 14일로 산출한다. 5555

## ---

**2\. 주요 모듈 설계 (Module Design)**

시스템은 크게 **스케줄 엔진**, **워크플로우 엔진**, **알림 엔진**으로 나뉩니다.

### **2.1 Backend: 핵심 클래스 및 파일 단위 설계**

#### **① ProjectManager (Domain Logic)**

* **파일**: src/modules/project/project.manager.ts  
* **역할**: 프로젝트 생성 시 런칭일을 기준으로 역산하여 마일스톤(기획, 채용, 시작일)을 생성합니다. 6666

* **주요 메서드**:  
  * calculateMasterSchedule(launchDate: Date): 20주 역산 알고리즘 실행. 777

  * applyLearningCurve(episodeNumber: number): 회차별 제작 기간(14일 vs 7일) 반환. 8888

#### **② WorkflowEngine (Relay Logic)**

* **파일**: src/modules/workflow/workflow.engine.ts  
* **역할**: '배경-선화-채색-후보정'으로 이어지는 페이지 단위 바통 터치를 관리합니다. 999

* **주요 메서드**:  
  * completeTask(pageId: string, taskType: TaskType): 특정 공정 완료 처리. 10

  * unlockNextTask(pageId: string): 배경 완료 시 선화를 READY로 변경하는 트리거. 11

#### **③ SafetyGuardService (Monitoring)**

* **파일**: src/modules/monitor/safety-guard.service.ts  
* **역할**: 런칭 1달 전 7화 봉인 여부 및 런칭일 10화 확보 상태를 실시간 체크합니다. 12121212

* **주요 메서드**:  
  * checkSealingStatus(projectId: string): 7화 봉인일(D-30) 준수 여부 확인. 13131313

  * predictBufferShortage(): 현재 속도 기반 비축분 부족 가능성 예측. 14

## ---

**3\. 데이터 모델 설계 (Database Entities)**

관계를 명확히 하기 위한 핵심 Entity 구조입니다.

| Class (Table) | Fields | Description |
| :---- | :---- | :---- |
| **Project** | id, launchDate, velocityConfig | 전체 일정 및 7+3 전략의 기준점 15151515  |
| **Episode** | id, projectId, epNumber, dueDate | 2주 적응기 로직이 적용되는 대상 16161616  |
| **Page** | id, episodeId, pageNumber | 20,000px 단위의 실제 작업 단위 17  |
| **Task** | id, pageId, type, status | 배경, 선화 등 공정의 상태 및 의존성 18  |

## ---

**4\. 비즈니스 프로세스 흐름 (Sequence)**

1. **Initialize**: PD가 런칭일을 입력하면 ProjectManager가 기획 시작일(2026-06-15)부터 7화 봉인일(2026-12-21)까지의 마스터 플랜을 DB에 적재합니다. 19191919

2. **Hiring**: 기획 완료 후 5주간 채용 기간을 블로킹 처리합니다. 20202020

3. **Production**: 제작 개시(2026-09-14) 시 WorkflowEngine이 Ep 01, Page 1의 '배경' 공정을 활성화합니다. 212121212121212121

4. **Relay**: 배경 작가가 '완료'를 누르면 WorkflowEngine이 즉시 선화 작가에게 알림을 보내고 작업 권한을 부여합니다. 22

---

